<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".HomeActivity">

    <TextView
        android:id="@+id/home_started_title"
        android:layout_width="331dp"
        android:layout_height="508dp"
        android:layout_marginTop="132dp"
        android:text="@string/started_title"
        android:textColor="@color/primary"
        android:textSize="30sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/home_second_title"
        android:layout_width="236dp"
        android:layout_height="93dp"
        android:layout_marginTop="200dp"
        android:layout_marginEnd="124dp"
        android:text="@string/second_title"
        android:textColor="@color/primary"
        android:textSize="25sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.348"
        app:layout_constraintStart_toStartOf="@+id/home_started_title"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/logout_button"
        android:layout_width="246dp"
        android:layout_height="46dp"
        android:text="@string/logout_button"
        android:textColor="@color/white"
        android:textSize="15sp"
        app:backgroundTint="@color/shadow_primary"
        app:layout_constraintBottom_toBottomOf="@+id/home_started_title"
        app:layout_constraintEnd_toEndOf="@+id/home_started_title"
        app:layout_constraintStart_toStartOf="@+id/home_started_title" />


</androidx.constraintlayout.widget.ConstraintLayout>